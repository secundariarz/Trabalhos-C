//Ler 4 números inteiros e calcular a soma dos que forem par.


#include <stdio.h>

int main() {
    int a, i ;

    for(i = 0; i < n; i++) {
        scanf("%d", &a);
        if(a % 2 == 0) {
            prinft("%d", a);
        }
    }
}